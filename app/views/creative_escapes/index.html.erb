<h1>creative escapes list</h1>

<% @images_test = cl_image_tag("test", width: 1150, height: 300, crop: :fill) %>

<div class="container">
  <div class="row">

    <% @creative_escapes.each do |creative_escape| %>
<div class="col-xs-12 col-sm-4">
      <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('background.jpg');"> -->
        <div class="card-category">maw</div>
          <%#= cl_image_tag("test", width: 1150, height: 300, crop: :fill) %>
          <div class="card-description">
           <h2><%= creative_escape.skill %></h2>
           <p>Embroidery in Paris</p>
          </div>
            <a class="card-link" href="#" ></a>
      </div>
    </div>
    <% end %>
